[project]
name = "ddnet-mcpserver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.2.3",
    "mcp>=1.6.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
]
